version: '3.1'
services:
  postgres:
    build: .
    restart: always
    environment:
      POSTGRES_USER: ouaf
      POSTGRES_PASSWORD: ouaf
      POSTGRES_DB: cards
    ports:
      - 5432:5432
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
